# Configuración del servidor
NODE_ENV=development
PORT=3000

# Configuración de la base de datos
DB_HOST=localhost
DB_PORT=3306
DB_NAME=portafolio_docente
DB_USER=root
DB_PASSWORD=

# JWT Secret
JWT_SECRET=portafolio_docente_secret_key_2025_unsaac
JWT_EXPIRES_IN=24h

# Configuración de archivos
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
ALLOWED_EXTENSIONS=pdf,doc,docx,ppt,pptx,xls,xlsx,png,jpg,jpeg,gif,txt,zip,rar

# Configuración de correo (opcional para notificaciones)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASS=
MAIL_FROM=portafolio@unsaac.edu.pe

# Configuración de almacenamiento en la nube (futuro)
CLOUD_STORAGE_PROVIDER=local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_BUCKET_NAME=

# Configuración de logs
LOG_LEVEL=info
LOG_FILE=./logs/app.log